<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="DottedTextView">
        <attr name="android:text" />
        <attr name="dotVisibility">
            <!-- Visible on screen; the default value. -->
            <enum name="visible" value="0" />
            <!-- Not displayed, but taken into account during layout (space is left for it). -->
            <enum name="invisible" value="1" />
            <!-- Completely hidden, as if the view had not been added. -->
            <enum name="gone" value="2" />
        </attr>
    </declare-styleable>

    <declare-styleable name="CMCheckBox">
        <attr name="android:text" />
    </declare-styleable>

    <declare-styleable name="DropdownView">
        <attr name="title" format="string" localization="suggested" />
        <attr name="dropdownText" format="string" localization="suggested" />
    </declare-styleable>

    <declare-styleable name="CircleStepIndicator">
        <attr name="currentStep" format="integer" />
        <attr name="maxSteps" format="integer" />
    </declare-styleable>

    <attr name="labelId" format="string" />

    <declare-styleable name="LabelTextView">
        <attr name="labelId" />
        <attr name="android:text" />
    </declare-styleable>

    <declare-styleable name="LabelButton">
        <attr name="labelId" />
        <attr name="android:text" />
    </declare-styleable>

</resources>